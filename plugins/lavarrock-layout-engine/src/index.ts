/**
 * Layout Engine plugin — resource layer.
 *
 * Provides layout types, presets, CSS variable resolution,
 * DOM application, and serialisation without any UI.
 * The Layout Manager plugin depends on this for the UI pane.
 */
export * from "./lib/layout-engine";
