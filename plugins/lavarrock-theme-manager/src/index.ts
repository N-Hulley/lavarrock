/**
 * Theme Manager — shared plugin (no pane).
 * Registers all theme settings with the Settings Engine.
 */
export { initThemeManager } from "./init";
export * from "@lavarrock/plugin-theme-engine";
