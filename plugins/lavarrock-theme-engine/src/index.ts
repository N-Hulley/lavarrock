/**
 * Theme Engine plugin — resource layer.
 *
 * Provides the core theme engine (types, attribute definitions, colour
 * helpers, serialisation, DOM application) without any UI.
 * The Theme Manager plugin depends on this for the UI pane.
 */
export * from "./lib/theme-engine";
export * from "./lib/theme-utils";
