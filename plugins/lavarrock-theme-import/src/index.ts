/**
 * Theme Import — shared plugin.
 * Registers a custom field renderer for VS Code theme import.
 */
export { initThemeImport } from "./components/ThemeImportExtension";
export { default } from "./components/ThemeImportExtension";
